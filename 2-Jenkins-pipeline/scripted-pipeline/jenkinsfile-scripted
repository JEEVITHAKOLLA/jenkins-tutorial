node {
    stage('Build') {
        // Run build commands
        sh 'echo "Building project..."'
    }
    stage('Test') {
        // Run tests
        bat'echo "Running tests..."'
    }
    stage('Deploy') {
        bat 'echo "Deploying application..."'
    }
}
